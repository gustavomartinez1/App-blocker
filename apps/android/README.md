# Android

Por ahora contiene sólo el motor de reglas portado a Kotlin (`engine/`) con pruebas unitarias que reproducen las de `packages/core`, incluido el vector de los códigos sin conexión.

Falta la app en sí (interfaz, vinculación, sincronización y aplicación de las reglas en el sistema).
